<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini-Project</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end w-full ">
            <a class="bg-red-500 rounded-md px-3 py-2 p-10 inline-block" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">You can edit your post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind" class=" block p-2 outline-none resize-none bg-transparent border-2 border-zinc-500 rounded-md w-1/3" name="content"><%= post.content%></textarea>
            <input class="px-3 w-40 py-2 bg-yellow-500 text-black text-md rounded-md block mt-2" type="submit" value="Update Post">
        </form>


       
    </div>
</body>
</html>